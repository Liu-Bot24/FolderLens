# 25efa9f 审计修复验证

Pro 对固定版本 `25efa9f` 返回 2 条 P1、6 条 P2。以下为本地修复与验证结果，不代表新一轮审计通过，也不代表真实归档刷新问题已全部定位。

| 意见 | 修改 | 验证 |
|---|---|---|
| N1 集合回调期间数量不一致 | 先更新组与展平数量，再通知；索引器拒绝越界 | 原生集合 27 次回调，增删组、Reset、边界读取全部一致 |
| N2 首批选中后失败不再自动升级 | 扫描与元数据完成入口使用相同的完整结果锁定条件 | 两个实际完成入口均可升级并保留选择 |
| N3 重试丢弃仍有效的缩略图 | 请求按根、版本、行身份、当前消费者归属校验 | 首批解码暂停期间失败和显式重试，解码结果继续显示 |
| N4 旧选择恢复覆盖新选择 | 恢复前核对选择请求序号 | 发布之后的新点选仍拥有预览 |
| N5 当前项随序号漂移 | 保留 CurrentItem 身份，删除后清空，发出不可取消的变更通知 | 前方插入、移除、Reset 后当前项及通知正确 |
| N6 收藏视图覆盖分类偏好 | 仅真实视图按钮更新分类偏好 | 恢复收藏后离开、返回 PDF 仍使用用户网格偏好 |
| N7 日期快捷项未覆盖同日小时范围 | 记录显式预设操作 | 今天替换为全天；未操作仍保留原精确时间 |
| N8 缺少通配符 | 增加独立通配符模式及 UI、规则协议 | 名称、相对路径、大小写、子目录、暂停、保存与 SQL 查询一致 |

通配符 `*` 匹配零个或多个字符，`?` 匹配一个字符，均不跨目录分隔符；整个名称或相对路径参与匹配。子目录由“包括子文件夹”单独控制。普通匹配模式不隐式解释通配符。

Release 构建通过；全套单元测试 **238 通过、0 失败、0 跳过**。隔离原生验证包括上述交错情形、首批失败保留、分组折叠及大组发布。首批、集合与筛选问题均先用旧行为复现失败，再验证修复。

失败日志补充候选编号、资源失败分类、耗时、集合通知上下文和 Reset 计数。大变更可触发标准 Reset；数量是实际通知数，不承诺整个发布只有一次 Reset。

限制：没有重开用户安装版或真实归档；没有替换安装文件。耳机实际拔插、物理多屏迁移、网络位置及完整规模验收不由这些隔离验证代替。
