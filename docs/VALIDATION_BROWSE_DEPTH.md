# 浏览层级与预览操作

- 路径栏旁的“查看层级”默认所有层级，提供 1–5 层及自定义整数层数。当前文件夹算第 1 层；改变层级不重新扫描。
- 文件搜索只匹配文件名，已移除搜索路径的勾选。旧保存视图也不能重新开启路径匹配。
- 格式文字和格式弹层沿左边缘对齐。窗口预览、全屏预览移至左侧预览区域；100% 按钮改为内容宽度，避免文字裁切。

## 验证边界

Release 构建通过：0 错误、7 个 NU1900 包漏洞源不可达警告。全量单元测试 347 项通过、0 失败、0 跳过；包含本批 3 项新增测试及原有查询随机对照测试。

小型合成数据覆盖宇宙/银河系/太阳系/地球和同级分支，包含图片、视频、普通文件。核对 1、3、4、所有层级的首屏与完整快照、目录范围重设、分组、收藏夹不受层级限制、旧路径搜索忽略、保存恢复和非法层数。修改前 1 层用例失败，修复后通过。

原生验证增加格式对齐、两种预览入口位置、100% 内容宽度和旧设置恢复断言，但没有启动窗口；真实鼠标、缩放比例与观感仍须实际验收，不以构建代替。
