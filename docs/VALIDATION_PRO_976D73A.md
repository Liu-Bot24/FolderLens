# 976d73a 复审修复验证

日期：2026-09-23。基线：`976d73ade2b41b3f2779cfd1c94a6feb5daaf9d4`。

复审判定 A09-02、A09-04、A09-05 的原缺陷源码级闭合；本次处理剩余 REV-01 至 REV-05，不撤销已成立的修复。

| 编号 | 修复 | 反例与正常路径 |
| --- | --- | --- |
| REV-01 | 未取得文件身份不提交强绑定 | 扫描与单文件身份均为空、父目录身份已知的用例原先错误接受；现在拒绝。正常无 ID 枚举、单文件可取得身份仍能实际解码 |
| REV-02 | 跨请求归属保留内容身份对照，变化时事务内递增版本并清退旧元数据 | 同长度和时间的 A→B，经根代次或位置修订重新绑定：旧版本拒绝、尺寸及详情清空、ready 状态移除、迟到旧结果不能写入。只是重开未变化目录仍保留元数据 |
| REV-03 | 水合前在工作进程锁定并核验父目录链 | 父目录替换反例原先触发一次水合接缝，现在为零。正常目标触发一次；父目录和祖先目录在租约期间不能改名，释放后可改名 |
| REV-04 | 文件操作完成核对与全范围扫描停止状态分离 | 自有夹具经真实 Shell 无界面复制、移动完成后，列表正确增加、移除文件；扫描令牌和任务保持停止，无关子目录不加载。旧目录完成刷新仍不能污染新目录 |
| REV-05 | 默认拖动动作使用属性级卷查询 | deferred 文件同卷默认移动，Ctrl 复制、Shift 移动，未知源卷复制；普通 stat 仍拒绝未批准占位内容，属性级卷请求可返回卷且不改变 deferred 属性 |

## 执行结果

- 最终全量测试：**620 通过，0 失败，0 跳过**。
- Release 构建成功，0 错误；3 条 NU1900 为漏洞数据源不可达，不表示漏洞检查已通过。
- 最终真实 WinUI 离屏六项：停止扫描后的 Shell 完成刷新、跨导航完成刷新、传入文件延迟取消、读取绑定解码、预览取消、真实 SMB 图库均 PASS。
- 原生复制/移动仅针对测试生成文件，通过实际 Shell 接口的无界面测试模式执行。本轮已越过上一轮仅检查提交边界的限制。
- 真实 SMB 图库保持正常读取；源文件只读，未用私人内容做外部上传。

自查中两个新增负例曾失败，均修正后重跑：仅属性目录句柄未阻止改名；普通单层脏目录策略仍会进入新发现子目录。最终分别使用参与共享检查的目录访问权、专用于已停止扫描完成核对的严格单层模式。没有通过取消这些断言取得 PASS。

本地忽略证据目录：`artifacts/rev-976d73a-20260923/`。报告副本 `pro-review.md`；红测 `corrected-red.log`、`hydration-red.log`、`stopped-shell-red/native-refresh.json`；目录锁反例 `full-tests.log`；严格范围反例 `final-stopped-shell-refresh/native-refresh.json`；最终 `full-tests-verified.log`、`scoped-build.log`、各 `verified-*/native-refresh.json`。最初 `initial-red.log` 中元数据用例有夹具约束错误，不能当成产品红测；已修正夹具并保留真正反例。

## 保留边界

真实云提供程序下载、目录共享及权限矩阵仍 **NOT_RUN**；水合接缝证明调用前授权及目录约束，不能替代提供程序实测。实际前台 Explorer 最终选中、进入 Shell 后的原生不可取消边界、多屏/DPI、完整网络矩阵和 A054a 长稳未因此闭合。干净 Windows/低内存仍按用户决定 **SKIP**。本次不是一期完成或发布 GO 的结论。
