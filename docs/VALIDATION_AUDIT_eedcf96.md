# eedcf96 审计修复验证

本次修复覆盖审计 F01–F08。以下验证在 Windows x64 开发环境执行，不构成正式发布 GO。

| 问题 | 修正 | 已执行验证 |
|---|---|---|
| F01 非法基本筛选异常 | 在查询、扫描和导航变更前校验完整候选；错误保留已接受结果 | 原生非法区间先复现异常，修复后区间、数值溢出、非法草稿导航及纠正后查询均通过 |
| F02 后续大批量刷新逐行通知 | 共享集合入口超过 4096 个增删项改为一次虚拟索引重置 | 真实 WinUI 对已有结果增删 10 万项；各一次通知，保留解码位图、选择和视口；单测验证更新自身不读取行 |
| F03 收藏身份补齐留下残留映射 | 同事务迁移当前位置实例的旧映射并去重；未恢复旧记录可分页查看、移除或明确重新收藏 | 复现一条成员变两条持久映射；修复后补齐、重叠根、重启、取消收藏、同名替换隔离通过；原生按钮操作通过 |
| F04 待判断视图文案错误 | 按原需求保留仅 Pending 语义，改为“仅显示尚未判定的文件” | 混合 Match/Pending 首屏与完整快照计数一致；随机查询差分通过 |
| F05 ready 缺值永久 Pending | 已读完但不可获得为 Unresolvable；明确无音轨不匹配音频编码条件 | 未读、unsupported、ready 缺时长、无音轨与 AAC 正例；NULL 保留且旧 epoch/version 拒绝 |
| F06 清空宽高下限无效 | 基本控件显式覆盖其负责的下限，保留高级上限 | 纯函数与真实控件恢复后清空验证通过 |
| F07 旧原生 DLL 混入新发布 | 在本次候选的新目录强制编译原生库，编译失败即拒绝 | 真实 CMake 夹具：旧 DLL 存在且源时间戳未变，修改源码仍生成新 DLL；已有目录与错误源码拒绝 |
| F08 大端 BigTIFF 缺失 | 识别两种字节序的 TIFF 42/43，验证 BigTIFF 偏移宽度及保留字 | 实际媒体工作进程四种 TIFF 组合均成功；旧代码仅大端 BigTIFF 失败；三种非法/截断头被拒绝 |

Release 全量构建零错误，532 项测试通过、0 跳过。全量测试日志为 `artifacts/audit-full-tests.log`，TRX 为 `artifacts/tests/audit-full/audit-full.trx`。原生与脚本证据分别位于 `artifacts/audit-filters-green`、`artifacts/audit-bulk-native`、`artifacts/audit-favorites-native2`、`artifacts/native-publish-test-20260916T065921749Z`。完整 startup 验证脚本也包含新增的三个原生回归入口。

NuGet 漏洞信息源不可达的 NU1900 警告仍存在，不能声称依赖漏洞扫描完成。未恢复记录的显式重绑定仅代表用户建立新收藏，不自动证明旧文件身份连续。测试中的 BigTIFF 为合成单像素样本，不能推广为所有 BigTIFF 变体均受支持。

物理键鼠、跨显示器 DPI、真实云提供方与 Explorer 集成、干净安装及完整性能验收仍未完成，正式发布结论保持 NO-GO。
