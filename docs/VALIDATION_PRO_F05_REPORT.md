# f05fde5 审计后续修复

审计固定基线为 `f05fde5484b257d43abd81f176b75601909d0e7f`。第一批修复已进入 `95e1c9a8f2cbbf2629fec9e92a296aa400a3eb1e`，对应证据见 [收藏状态回归](VALIDATION_COLLECTION_REVALIDATION.md)。

| 发现 | 处理与反例 |
|---|---|
| N1：身份 A→未知→B 继承收藏 | v6 独立保留最后确认身份，当前观察仍为未知；恢复 A 保留，B 清除；跨重启回归通过 |
| N2：旧选择复活已清除归属 | 事务内验证观察版本、路径修订、源根及状态，整批拒绝旧选择；菜单和快捷入口使用相同验证 |
| N3：无变化重扫反复算键 | NULL 安全变化条件阻止无效触发；真实重扫不再重写键，改名仍清收藏 |
| N4：目录改名扫描无关 Files | 根范围贯穿子树、退休、版本记忆、路径修改和清理；5,000 无关文件规模对照通过 |
| N5：Unicode 路径截取单位不一致 | 所有扫描路径 `substr` 的长度/偏移统一使用 SQL `length`，包括目录排除及队列更新；真实非递归扫描中补充平面字符红例两个失败，普通中文对照通过；修复后三个通过 |
| N6：扫描清收藏后侧栏不刷新 | 写连接 TEMP 触发器记录事务内成员修订；扫描进度按秒合并、完成/取消/失败收尾检查修订；只在实际变化后刷新侧栏和徽标，不替换冻结序列 |
| T1：WinPS 5.1 孤儿夹具找错宿主 | 生成的父脚本根据 PSEdition 选择当前宿主；5.1 红例未启动子夹具，修复后 PS7 和 5.1 均验证父退出、持管道后代回收及超时报告 |

N6 原生红例先确认侧栏旧数量；第一版修复侧栏后，冻结旧行星标仍失败。属性读取要求当前文件版本，不能拿旧行属性刷新代替徽标读取。现在单独批量读取收藏状态，并核对快照版本/路径修订，失效旧行明确清星；异步结果还检查当前刷新代次与行对象绑定，避免覆盖较新的结果。

证据：

- `artifacts/tests/pro-f05fde5/unicode-rename-red.trx`、`unicode-rename-green.trx`。
- `artifacts/pro-collection-refresh-red`、`pro-collection-refresh-green` 保留两次真实失败；`pro-collection-badges-green` 中扫描后计数/星标及冻结序列通过，原快捷规则继续通过。
- `artifacts/tests/pro-f05fde5/f05-final-report-unit.trx`：311 项通过，0 失败/跳过。
- `artifacts/pro-orphan-winps-red`、`pro-orphan-ps7-green`、`pro-orphan-winps-green`：孤儿验证返回受监督命令的预期超时 124，测试脚本确认后代已回收。
- `artifacts/pro-f05-report-native-final`：整套运行其余 37 项通过，新增收藏用例因已有扫描忙而未启动目标核对，数据库仍保留原路径和归属。测试随后增加等待已有扫描及数据库清除前提断言；`pro-collection-scan-ordered-1` 至 `-3` 连续三次通过。不是将这次 37/38 的原报告改成 38/38。

成员修订号只供本次运行中的 UI 合并刷新；TEMP 更新随事务回滚，不是新的持久化文件格式，也不改变 catalog v6。重复收藏、无变化操作和回滚不递增有效修订。初次启动仍读取完整收藏列表。

未验证边界继续保留：真实前台对话框 deferral/Hide、完整前台历史、真实豆包语音、云提供程序状态竞态、在途 CanvasBitmap/设备丢失、空闲 worker 内存压力恢复、完整归档长稳与干净机。WPR 系统采样尝试因 `0xc5585011` 权限不足未开始；不能记为 ETW 通过，也不能据此宣称其他程序卡顿已经解决。
